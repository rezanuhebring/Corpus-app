# FastAPI Backend Configuration
SECRET_KEY=YOUR_SECRET_KEY_WILL_BE_GENERATED_HERE
ACCESS_TOKEN_EXPIRE_MINUTES=1440

# --- REVOLUTIONARY CHANGE ---
# The backend will now connect to localhost because it shares the same network
# as the elasticsearch container.
ELASTICSEARCH_HOST=elasticsearch

# Production-only variables (for setup.sh)
CORPUS_DOMAIN=localhost
CERTBOT_EMAIL=your-email@example.com

# Agent Configuration (for reference)
AGENT_API_KEY=DEV_API_KEY_12345